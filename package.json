{
  "name": "rhc-meme-bot",
  "private": true,
  "version": "0.1.0",
  "description": "Robinhood Chain meme-coin trading bot: event listener, vetting, deployer graph, wallet tracker, decision and exit engines, with a paper/live execution gate.",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "workspaces": [
    "shared/*",
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n bot,ui -c cyan,magenta \"npm run dev -w @rhc/bot\" \"npm run dev -w @rhc/dashboard\"",
    "dev:bot": "npm run dev -w @rhc/bot",
    "dev:ui": "npm run dev -w @rhc/dashboard",
    "preflight": "npm run preflight -w @rhc/bot",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "22.13.5",
    "concurrently": "9.1.2",
    "tsx": "4.19.3",
    "typescript": "5.7.3",
    "vitest": "3.2.7"
  }
}
